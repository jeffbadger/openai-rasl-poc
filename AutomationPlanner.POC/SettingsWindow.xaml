<Window x:Class="AutomationPlanner.POC.SettingsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Settings" Height="320" Width="520">
    <Grid Margin="16">
        <Grid.ColumnDefinitions><ColumnDefinition Width="160"/><ColumnDefinition Width="*"/></Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/><RowDefinition Height="Auto"/><RowDefinition Height="Auto"/><RowDefinition Height="Auto"/><RowDefinition Height="Auto"/><RowDefinition Height="*"/><RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <TextBlock Text="OpenAI API Key"/><PasswordBox Grid.Column="1" x:Name="ApiKeyBox" PasswordChanged="ApiKeyBox_PasswordChanged"/>
        <TextBlock Grid.Row="1" Text="GPT Model" Margin="0,8,0,0"/><TextBox Grid.Row="1" Grid.Column="1" Text="{Binding Settings.Model}" Margin="0,8,0,0"/>
        <TextBlock Grid.Row="2" Text="Temperature" Margin="0,8,0,0"/><TextBox Grid.Row="2" Grid.Column="1" Text="{Binding Settings.Temperature}" Margin="0,8,0,0"/>
        <TextBlock Grid.Row="3" Text="Max Tokens" Margin="0,8,0,0"/><TextBox Grid.Row="3" Grid.Column="1" Text="{Binding Settings.MaxTokens}" Margin="0,8,0,0"/>
        <TextBlock Grid.Row="4" Text="Request Timeout" Margin="0,8,0,0"/><TextBox Grid.Row="4" Grid.Column="1" Text="{Binding Settings.RequestTimeoutSeconds}" Margin="0,8,0,0"/>
        <StackPanel Grid.Row="6" Grid.ColumnSpan="2" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="Save" Command="{Binding SaveCommand}" Click="Save_Click" Width="80" Margin="0,0,8,0"/>
            <Button Content="Cancel" Click="Cancel_Click" Width="80"/>
        </StackPanel>
    </Grid>
</Window>
