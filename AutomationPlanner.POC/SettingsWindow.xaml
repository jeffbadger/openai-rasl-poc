<Window x:Class="AutomationPlanner.POC.SettingsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Settings" Height="430" Width="640">
    <Grid Margin="16">
        <Grid.ColumnDefinitions><ColumnDefinition Width="180"/><ColumnDefinition Width="*"/><ColumnDefinition Width="Auto"/></Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <TextBlock Text="OpenAI API Key"/>
        <PasswordBox Grid.Column="1" Grid.ColumnSpan="2" x:Name="ApiKeyBox" PasswordChanged="ApiKeyBox_PasswordChanged"/>

        <TextBlock Grid.Row="1" Text="GPT Model" Margin="0,8,0,0"/>
        <TextBox Grid.Row="1" Grid.Column="1" Grid.ColumnSpan="2" Text="{Binding Settings.Model}" Margin="0,8,0,0"/>

        <TextBlock Grid.Row="2" Text="Temperature" Margin="0,8,0,0"/>
        <TextBox Grid.Row="2" Grid.Column="1" Grid.ColumnSpan="2" Text="{Binding Settings.Temperature}" Margin="0,8,0,0"/>

        <TextBlock Grid.Row="3" Text="Max Tokens" Margin="0,8,0,0"/>
        <TextBox Grid.Row="3" Grid.Column="1" Grid.ColumnSpan="2" Text="{Binding Settings.MaxTokens}" Margin="0,8,0,0"/>

        <TextBlock Grid.Row="4" Text="Request Timeout" Margin="0,8,0,0"/>
        <TextBox Grid.Row="4" Grid.Column="1" Grid.ColumnSpan="2" Text="{Binding Settings.RequestTimeoutSeconds}" Margin="0,8,0,0"/>

        <TextBlock Grid.Row="5" Text="Planner Package Folder" Margin="0,8,0,0"/>
        <TextBox Grid.Row="5" Grid.Column="1" Text="{Binding Settings.PlannerPackagePath, UpdateSourceTrigger=PropertyChanged}" Margin="0,8,8,0"/>
        <Button Grid.Row="5" Grid.Column="2" Content="Browse" Click="BrowsePlannerPackage_Click" Margin="0,8,0,0"/>

        <TextBlock Grid.Row="6" Text="Mock Data Base Folder" Margin="0,8,0,0"/>
        <TextBox Grid.Row="6" Grid.Column="1" Text="{Binding Settings.MockDataBasePath, UpdateSourceTrigger=PropertyChanged}" Margin="0,8,8,0"/>
        <Button Grid.Row="6" Grid.Column="2" Content="Browse" Click="BrowseMockData_Click" Margin="0,8,0,0"/>

        <TextBlock Grid.Row="7" Grid.Column="1" Grid.ColumnSpan="2" Text="Leave Mock Data Base Folder blank to use the selected planner package's mock-data folder." TextWrapping="Wrap" Margin="0,8,0,0" Foreground="DimGray"/>

        <StackPanel Grid.Row="8" Grid.ColumnSpan="3" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="Save" Click="Save_Click" Width="80" Margin="0,0,8,0"/>
            <Button Content="Cancel" Click="Cancel_Click" Width="80"/>
        </StackPanel>
    </Grid>
</Window>
